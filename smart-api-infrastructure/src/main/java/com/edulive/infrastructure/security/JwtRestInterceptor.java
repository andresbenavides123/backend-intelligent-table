package com.edulive.infrastructure.security;

/**
 * DESACTIVADO — Reemplazado por JwtAuthenticationFilter.java
 *
 * El HandlerInterceptor manual fue reemplazado por un OncePerRequestFilter de Spring Security,
 * que se integra correctamente con el SecurityContext y el SecurityFilterChain.
 *
 * Este archivo se conserva como referencia histórica pero no tiene efecto
 * porque la anotación @Component fue removida intencionalmente.
 */
// @Component  ← REMOVIDO: reemplazado por JwtAuthenticationFilter
public class JwtRestInterceptor {
    // Vacío — ver JwtAuthenticationFilter
}
